# AI PR Review — monorepo demo (Node + Python + Terraform)
name: AI PR Review

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  ai-review:
    uses: pulumamidi-harsha/agentic-review/.github/workflows/ai-review.yml@fa82daf
    with:
      review_type: quick
      pipeline_repository: pulumamidi-harsha/agentic-review
      pipeline_ref: fa82daf
    secrets:
      AI_API_KEY: ${{ secrets.AI_API_KEY }}
      AI_API_ENDPOINT: ${{ secrets.AI_API_ENDPOINT }}
      # Optional for public pipeline repo — falls back to github.token in the workflow
      ORG_PAT: ${{ secrets.ORG_PAT || github.token }}
